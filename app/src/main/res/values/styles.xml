<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="DarkStyle" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:background">@color/black</item>
        <item name="colorPrimary">@color/ametista</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Stile del testo -->
        <item name="android:textColor">@color/white</item>

        <!-- Stile dei pulsanti -->
        <item name="buttonStyle">@style/CustomButtonDark</item>
    </style>
    <!-- Stile personalizzato per i pulsanti -->
    <style name="CustomButtonDark" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="CustomToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextColor">@color/ametista</item>
        <item name="subtitleTextColor">@color/ametista</item>
        <item name="android:textColorPrimary">@color/ametista</item>
        <item name="android:textColorSecondary">@color/ametista</item>
    </style>

    <style name="LightStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/white</item>
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryDark">@color/lightGrey</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Stile del testo -->
        <item name="android:textColor">@color/black</item>

        <!-- Stile dei pulsanti -->
        <item name="buttonStyle">@style/CustomButtonLight</item>
    </style>

    <style name="CustomButtonLight" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/black</item>
        <item name="android:padding">10dp</item>
    </style>
</resources>